import Image from "next/image";
import Link from "next/link";

type GalleryPhotoProps = { image: string; title: string; description?: string };

export default function GalleryPhoto({ image, title, description }: GalleryPhotoProps) {
  return <figure><Link href="/catalogo" aria-label={`Ver produtos no catálogo: ${title}`} className="group/image relative block aspect-square w-full overflow-hidden rounded-2xl text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-vermelho"><Image src={image} alt={title} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw" className="object-cover transition-transform duration-200 group-hover/image:scale-105" /><span className="absolute inset-0 flex flex-col justify-end bg-marrom-escuro/75 p-5 text-white opacity-0 transition-opacity duration-200 group-hover/image:opacity-100 group-focus-visible/image:opacity-100 max-sm:opacity-100 max-sm:bg-gradient-to-t max-sm:from-marrom-escuro/75 max-sm:via-transparent max-sm:to-transparent"><span className="font-titulo text-lg font-bold">{title}</span>{description && <span className="mt-1 text-sm leading-6 text-white/85">{description}</span>}<span className="mt-3 text-sm font-bold text-dourado">Ver catálogo →</span></span></Link><figcaption className="mt-3 sm:hidden"><Link href="/catalogo" className="block rounded-md font-semibold text-marrom-escuro focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-vermelho">{title}<span className="mt-1 block text-sm font-normal text-vermelho">Ver catálogo →</span>{description && <span className="mt-1 block text-sm leading-6 text-marrom-escuro/75">{description}</span>}</Link></figcaption></figure>;
}
